import { PlusCircle } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';

export default function PublierPage() {
  return <PlaceholderPage icon={PlusCircle} subtitle="Publiez votre bien sur MEGGA" />;
}
