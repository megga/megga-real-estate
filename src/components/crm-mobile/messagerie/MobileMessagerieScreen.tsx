/**
 * MEGGA CRM mobile — Messagerie (lecture seule, D16).
 *
 * ⚠ PLACEHOLDER de la tâche 2.1 : `ResponsiveRoute` exige un composant mobile
 * pour monter `/dashboard/messagerie`, et l'écran minimal (liste des fils +
 * lecture, sans composer) est écrit en T2.14.
 */
export default function MobileMessagerieScreen() {
  return null
}
